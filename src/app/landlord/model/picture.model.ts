// Interfaz para representar una imagen de un nuevo listado
export interface NewListingPicture {
  file: File; // Archivo de imagen, que representa el archivo físico de la imagen
  urlDisplay: string; // URL para mostrar la imagen en la interfaz de usuario
}
