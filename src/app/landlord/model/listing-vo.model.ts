// Interfaz para el número de huéspedes
export interface GuestsVO {
  value: number; // Valor que representa la cantidad de huéspedes
}

// Interfaz para el número de habitaciones
export interface BedroomsVO {
  value: number; // Valor que representa la cantidad de habitaciones
}

// Interfaz para el número de camas
export interface BedsVO {
  value: number; // Valor que representa la cantidad de camas
}

// Interfaz para el número de baños
export interface BathsVO {
  value: number; // Valor que representa la cantidad de baños
}

// Interfaz para el título del listado
export interface TitleVO {
  value: string; // Valor que representa el título del listado
}

// Interfaz para la descripción del listado
export interface DescriptionVO {
  value: string; // Valor que representa la descripción del listado
}

// Interfaz para el precio del listado
export interface PriceVO {
  value: number; // Valor que representa el precio del listado
}
